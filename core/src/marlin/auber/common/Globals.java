package marlin.auber.common;

/**
 * A place for global variables that have nowhere else to go.
 */
public class Globals {
    /**
     * Whether there's something in the GUI that uses clicks, and
     * other parts of the game (e.g. arrest gun) should ignore clicks.
     */
    public static boolean blockClicks = true;
}
